# Q3 图件

Q3 图件由 `code/q3/problem3.py` 根据 `results/q3/` 的正式扫描和收敛数据生成：

- `q3_threshold_event.pdf`：连续最大含水率与阈值事件；
- `q3_threshold_profiles.pdf`：事件前后径向含水率剖面；
- `q3_convergence.pdf`：空间网格对事件时刻的影响。

- 新增阈值事件局部放大图（嵌入 `q3_threshold_event.pdf` 右下角）：用于正文说明首次达标时刻的根定位。
